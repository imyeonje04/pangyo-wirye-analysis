# 판교테크노밸리 vs 위례신도시 업무·상업용지 비교분석

스마트시티 기말고사 대체과제 — 판교와 위례 두 신도시를 인구·종사자·교통접근성·토지이용 측면에서 비교 분석한 웹 시각화 프로젝트입니다.

**핵심 논지**: 위례는 30분 등시간권 내 접근 가능 역수(88개)가 판교(67개)보다 많지만, 종사자 밀도는 465명/ha로 판교(1,391명/ha)의 약 1/3 수준이고 직주비도 0.65로 판교(1.53)에 크게 못 미칩니다. 활성화 저조의 핵심 원인은 교통접근성 부족이 아니라 **토지이용 규제·앵커기업·기업생태계 부재**로 해석하였습니다.

---

## 최종 산출물

| 파일 | 설명 |
|------|------|
| `판교와위례비교분석.html` | 브라우저에서 바로 열 수 있는 단일 HTML (데이터 인라인 내장) |

- 별도 빌드·서버 불필요
- Leaflet.js, Chart.js를 CDN으로 불러오므로 인터넷 연결 필요
- GitHub Pages: 이 파일을 `index.html`로 복사 후 Pages 소스로 지정

---

## 저장소 구조

```
(저장소 루트)
│
├── README.md
├── 판교와위례비교분석.html              ← 최종 제출 페이지
│
└── 기말고사대체과제프로젝트/
    │
    ├── fetch_landuse.py               ← [전처리] VWorld WFS API → 용도지역 GeoJSON
    ├── files/
    │   ├── geocode_biz.py             ← [전처리] 건축물 주소 지오코딩 → 밀집구역 폴리곤
    │   └── biz_buildings.csv          ← geocode_biz.py 입력: 업무·상업 건물 목록
    │
    ├── 인구.종사자데이터파일/           ← [원천] 인구·사업체 통계 CSV
    │   ├── 31021_*/                   ← 성남시 분당구
    │   ├── 31023_*/                   ← 성남시 수정구
    │   └── 31180_*/                   ← 서울시 송파구
    │
    ├── 지역경계파일/                    ← [원천] 읍면동 경계 Shapefile (2025년 2Q)
    │
    ├── 건축물대장_andy0575_20260619002319.csv  ← [원천] 건축물대장 전체
    │
    ├── subway_network (1)/            ← [원천 + 전처리] 수도권 지하철 네트워크
    │   ├── README.md                  ← 지하철 데이터 상세 스키마 설명
    │   ├── edit/                      ← QGIS 수작업 편집 원본 (stations/lines/transfer shp)
    │   ├── make_network.ipynb         ← [전처리] shp → 그래프 변환 노트북
    │   ├── network/nodes.tsv, links.tsv  ← 노트북 출력 (역 노드·링크)
    │   ├── line_waits.parquet         ← 노선별 배차간격(초)
    │   └── opening.tsv                ← 노선 개통 시점 목록
    │
    ├── pangyo_wirye_data/             ← [가공 완료] HTML이 사용하는 공간 데이터
    │   ├── zones.geojson              ← 판교·위례 구역계 경계
    │   ├── zones_precise.geojson      ← 건축물대장 기반 업무·상업 밀집구역 폴리곤
    │   ├── isochrones.geojson         ← 30분/60분 등시간권 폴리곤
    │   ├── accessibility_curves.json  ← 0~60분 누적 접근 가능 역수 곡선
    │   ├── zone_stats.json            ← 인구·종사자·직주비·밀도 핵심 지표
    │   ├── landuse_v2.geojson         ← 용도지역 폴리곤
    │   ├── buildings_colored.geojson  ← 판교 건물 용도별 색상 레이어
    │   └── songpa_wirye_colored.geojson ← 위례(송파) 건물 용도별 색상 레이어
    │
    └── pangyo_wirye_github_pages/     ← GitHub Pages 배포용 (별도 버전)
```

---

## 데이터 처리 흐름

```
[원천 데이터]                         [처리 스크립트]               [가공 결과]
─────────────────────────────────────────────────────────────────────────────
건축물대장.csv
  └→ files/biz_buildings.csv ───→ files/geocode_biz.py ────→ zones_precise.geojson

VWorld WFS API ────────────────→ fetch_landuse.py ──────→ landuse_v2.geojson

인구·종사자 CSV                   (수작업 집계)              zone_stats.json
지역경계 Shapefile                (수작업 변환)              zones.geojson

지하철 edit/*.shp
  + line_waits.parquet ─────→ make_network.ipynb ──→ network/nodes.tsv / links.tsv
  + opening.tsv                                              ↓ (Dijkstra)
                                                     isochrones.geojson
                                                     accessibility_curves.json

                      [가공 결과 전체]
                             ↓
                판교와위례비교분석.html  ← 최종 단일 HTML
```

---

## 분석 목적

1. 업무·연구·판매 기능은 실제로 어느 정도 집적되어 있는가?
2. 용도지역 계획과 실제 건축물 이용은 어떻게 다른가?
3. 철도 접근성이 높으면 업무 집적도 함께 높아지는가?
4. 상주인구와 종사자 구성은 두 지역에서 어떻게 다른가?
5. 도로망, 버스정류장 및 역세권 공급에는 어떤 차이가 있는가?

---

## 주요 결과

| 지표 | 판교테크노밸리 | 위례신도시 |
|------|:--------------:|:----------:|
| 분석경계 면적 | 87.3 ha | 150.0 ha |
| 업무·연구·판매 분류 레코드 | 79건 | 26건 |
| 건축물대장 업무시설 연면적 | 182.1만㎡ | 59.7만㎡ |
| 총종사자 | 91,806명 | 13,028명 |
| 직주비 | **1.53** | 0.65 |
| 종사자 밀도 | **1,391명/ha** | 465명/ha |
| 30분 이내 도달 가능 역 | 67개역 | **88개역** |
| 평균 유효 용적률 | 314.0% | 219.6% |
| 토지이용 혼합도(LUM) | 0.415 | 0.767 |

> 건축물 수치는 물리적으로 중복 제거된 건물 동수가 아니라 건축물대장 용도 분류 레코드 수입니다.

---

## 분석 범위

- **판교**: 판교테크노밸리 도시지원시설용지 중심 87.3 ha (성남시 분당구·수정구)
- **위례**: 업무·상업 기능과 준주거지역을 포함한 분석경계 150.0 ha (서울시 송파구)
- 위례 상업지역 토지면적 44.2 ha는 전체 분석경계와 구분하여 해석하였습니다.

---

## 사용 기술

| 분야 | 도구 |
|------|------|
| 지리 데이터 처리 | Python, GeoPandas, Shapely |
| 지하철 네트워크 분석 | Jupyter Notebook, SciPy (Dijkstra) |
| 공간 데이터 편집 | QGIS |
| 웹 시각화 | Leaflet.js, Chart.js (CDN) |
| 지오코딩 | VWorld 주소 API |

---

## 원천 데이터 출처

| 데이터 | 출처 | 기준 시점 |
|--------|------|-----------|
| 인구총괄·성연령별 인구 | 행정안전부 주민등록 인구통계 | 2024년 |
| 사업체·종사자 수 | 통계청 전국사업체조사 (산업 대·중분류) | 2023년 |
| 읍면동 경계 Shapefile | 통계청 통계지리서비스 (SGIS) | 2025년 2분기 |
| 건축물대장 | 세움터(건축행정시스템) | 2026년 6월 |
| 토지이용계획(용도지역) | VWorld WFS API (`lt_c_uq111`) | 국토교통부 |
| 지하철 역·노선·환승 | QGIS 수작업 편집 (OSM·KRRI 참고) | 2026년 5월, 1호선~GTX-C |
| 전국 버스정류장 | 국토교통부 | 2025년 10월 |
| 도로망 | OpenStreetMap | 2026년 6월 조회 |

원자료의 저작권과 이용조건은 각 제공기관의 정책을 따릅니다.

---

## 재현 방법

### 1. 환경 준비

```bash
pip install geopandas pyogrio pandas pyarrow shapely requests jupyterlab
```

### 2. 지하철 네트워크 그래프 생성

```bash
cd "기말고사대체과제프로젝트/subway_network (1)"
jupyter lab
# make_network.ipynb 를 셀 순서대로 실행
# → network/nodes.tsv, network/links.tsv 생성
```

### 3. 용도지역 데이터 수집

```bash
# fetch_landuse.py 의 API_KEY 를 본인 VWorld 키로 교체 후 실행
python 기말고사대체과제프로젝트/fetch_landuse.py
```

### 4. 업무·상업 건물 지오코딩

```bash
# geocode_biz.py 의 API_KEY 를 본인 VWorld 키로 교체 후 실행
python 기말고사대체과제프로젝트/files/geocode_biz.py
```

### 5. 최종 결과 확인

`판교와위례비교분석.html`을 브라우저에서 열면 됩니다.

---

## 전처리 과정

1. 분석경계를 정의하고 면적 계산용 좌표계 EPSG:5179로 변환했습니다.
2. 건축물대장을 좌표화하여 경계와 결합하고 주용도를 재분류했습니다.
3. VWorld 용도지역을 경계로 잘라 용도별 면적과 비율을 계산했습니다.
4. SGIS 집계구와 경계의 교차면적 비율로 인구·가구·사업체·종사자를 추정했습니다.
5. 판교역과 복정역을 출발점으로 Dijkstra 알고리즘으로 철도 도달역을 계산했습니다.
6. 도달역 800m 버퍼 합집합과 집계구를 결합해 도달 가능 인구·종사자를 추정했습니다.
7. 역 500m·1km 버퍼와 경계를 교차해 역세권 면적 비율을 계산했습니다.
8. OSM 도로를 경계로 잘라 도로망 밀도와 최근접 IC 직선거리를 계산했습니다.
9. 버스정류장 중복 위치를 정리하고 경계 내부 정류장 수와 밀도를 계산했습니다.
10. 결과를 GeoJSON·JSON으로 저장하고 최종 HTML에 인라인으로 내장했습니다.

---

## 산출 기준

- 면적 및 거리 계산 좌표계: EPSG:5179
- 인구·종사자: 집계구 교차면적 비율을 이용한 면적가중 추정
- 도달역: 환승 노선의 동일 물리적 역 통합
- 도달 생활권: 도달역별 직선거리 800m 버퍼 합집합
- 역세권: 운영역의 500m·1km 직선거리 버퍼
- 도로망 밀도: 경계 내부 도로 길이 ÷ 분석경계 면적
- 토지이용 혼합도: 고정 용도 범주의 정규화 Shannon entropy
- 유효 용적률: 0% 초과 2,000% 미만 레코드

---

## 분석 한계

- 집계구 통계는 건물별 실제 값이 아닌 교차면적 비례 추정치입니다.
- 철도 접근성은 사용한 네트워크와 환승시간 가정에 영향을 받습니다.
- 역세권·버스 생활권은 직선거리 버퍼로 실제 보행경로와 다를 수 있습니다.
- OSM 자료는 조회 시점의 편집 상태에 영향을 받습니다.
- 건축물대장 분류 레코드는 물리적 건물 동수와 같지 않습니다.

---

## 데이터 출처 링크

- [SGIS 통계지리정보서비스](https://sgis.kostat.go.kr/)
- [VWorld 공간정보 오픈플랫폼](https://www.vworld.kr/)
- [건축HUB (세움터)](https://hub.go.kr/)
- [OpenStreetMap](https://www.openstreetmap.org/)
- [LH 토지주택연구원](https://lhri.lh.or.kr/web/rsrchReprt/RsrchReprtForm.do?reprtId=1870)